pipeline {
    agent any // 1. Where to run (which worker/node)
    
    stages {
        stage('Build') {
            steps {
                echo 'Building application...'
            }
        }
        stage('Test') {
            steps {
                echo 'Running unit tests...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Running unit tests...'
            }
        }
    }
}
